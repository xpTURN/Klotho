; Klotho's runtime is a C# library — it works via the addons/klotho Klotho.props import (or NuGet)
; whether or not this plugin is enabled. The EditorPlugin (plugin.gd) is a no-op that only registers
; the addon under Project Settings > Plugins. License: Apache-2.0.
[plugin]
name="Klotho"
description="Deterministic lockstep / rollback netcode for Godot (.NET)."
author="xpTURN"
version="0.5.3"
script="plugin.gd"
